<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/security
		http://www.springframework.org/schema/security/spring-security.xsd
		http://www.springframework.org/schema/context
		http://www.springframework.org/schema/context/spring-context.xsd">

	<!-- OAuth2 Properties 로드 -->
	<context:property-placeholder location="classpath:oauth.properties" ignore-unresolvable="true"/>

	<!-- HTTP Firewall 설정 (세미콜론 허용) -->
	<beans:bean id="httpFirewall" class="org.springframework.security.web.firewall.StrictHttpFirewall">
		<beans:property name="allowSemicolon" value="true"/>
	</beans:bean>

	<http-firewall ref="httpFirewall"/>

	<!-- Spring Security 비활성화 (OAuth는 수동으로 처리) -->
	<http pattern="/resources/**" security="none"/>
	<http pattern="/login.do" security="none"/>
	<http pattern="/oauth2callback.do" security="none"/>
	<http pattern="/oauth2/google.do" security="none"/>
	<http pattern="/gis.do" security="none"/>

	<http auto-config="true" use-expressions="true">
		<intercept-url pattern="/**" access="permitAll" />
		<csrf disabled="true"/>
	</http>

	<authentication-manager>
	</authentication-manager>

</beans:beans>
